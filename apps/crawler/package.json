{
  "name": "@home-fit-ai/crawler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:zip": "node esbuild.config.mjs && cd dist && zip -r index.zip index.mjs",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "3.679.0",
    "cheerio": "1.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.145",
    "@types/node": "^24.0.0",
    "esbuild": "0.24.0",
    "tsx": "4.19.2",
    "typescript": "^5.4.5"
  }
}
